import React from 'react'

const WeaknessType = ({weaknesses}) => {
   
  return (
    // <>
    // {weaknesses.length === 0 ? "No weakness" : 
    //     weaknesses.map((weakness) => <span className="weakness">{weakness}</span>)
    // }
    // </>
    <span>{weaknesses}</span>
  )
}

export default WeaknessType